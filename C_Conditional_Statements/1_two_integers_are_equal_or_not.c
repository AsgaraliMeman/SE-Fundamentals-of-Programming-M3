#include<stdio.h>
main()
{
	int a,b;
	
	printf("Enter two number :   ");
	scanf("%d %d",&a,&b);
	
	if (a==b)
	 printf("Both number are equal ");
	 
	else 
	 printf("Both number are not equal ");
	
}
